service: cfn-templates-lambda-helpers

provider:
  name: aws
  runtime: nodejs4.3
  deploymentBucket: ${env:DEPLOYMENT_BUCKET}
  stage: ${env:DEPLOYMEMT_STAGE}

functions:
  deployFrontendAction:
    handler: handler.deployFrontendAction
    memorySize: 1024
    timeout: 300
    role: DeployFrontendFunctionRole

resources:
  Resources:
    DeployFrontendFunctionRole:
      Type: AWS::IAM::Role
      Properties:
        RoleName: CloudFormationTemplatesDeployFrontendFunctionRole-${env:DEPLOYMEMT_STAGE}
        AssumeRolePolicyDocument:
          Version: "2012-10-17"
          Statement:
            Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
        ManagedPolicyArns:
          - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
          - arn:aws:iam::aws:policy/AdministratorAccess